interface ChatInputProps {
  isDisable?: boolean;
}

const ChatInput = ({ isDisable }: ChatInputProps) => {
  return (
    <div>
      <div>chat input</div>
    </div>
  );
};

export default ChatInput;
